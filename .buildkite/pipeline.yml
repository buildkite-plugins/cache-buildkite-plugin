steps:
  - label: ":shell: Tests"
    plugins:
      docker-compose#v3.13.0:
        run: tests

  - label: ":shell: Shellcheck"
    plugins:
      shellcheck#v1.1.2:
        files: hooks/**
        options: "-x"

  - label: ":sparkles: Lint"
    plugins:
      plugin-linter#v3.0.0:
        id: cache
